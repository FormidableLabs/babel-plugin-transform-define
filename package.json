{
  "name": "babel-plugin-transform-replace",
  "version": "1.0.0",
  "description": "Statically replace and evaluate expressions in Babel",
  "author": "Michael Jackson",
  "license": "MIT",
  "main": "lib",
  "scripts": {
    "build": "rimraf lib && babel ./src -d lib",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "rimraf": "^2.5.2"
  },
  "babel": {
    "presets": [ "es2015" ]
  }
}
